import React from "react";
import style from "./style.module.scss"

const BingMap = ({  }) => {
 
  return (() => {

    
    return (
      <div key={index}>
        
      </div>
    );
  });
};

export default BingMap;